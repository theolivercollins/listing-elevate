import { LECyclingWord } from "reelready";

export const Default = () => (
  <div style={{ fontSize: 40, fontWeight: 700, padding: 24 }}>
    Sell listings{" "}
    <LECyclingWord words={["faster", "cinematically", "for more"]} />
  </div>
);
