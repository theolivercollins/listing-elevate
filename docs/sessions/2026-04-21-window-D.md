# Window D — Router-Table Aggregation Session Log

Date: 2026-04-21
Branch: `session/router-2026-04-21`
Worktree: `.worktrees/wt-router`
Brief: `docs/briefs/2026-04-21-window-D-router-aggregation.md`

## Mission

Build `scripts/build-router-table.ts` that aggregates every rating across (legacy Lab, Phase 2.8 listings Lab, prod scene_ratings), applies the Phase B winner rule (≥3 iter + ≥80% 4★+ per (room × movement) bucket), and emits:

1. `lib/providers/router-table.draft.ts` (DRAFT — not wired)
2. `docs/audits/router-coverage-2026-04-21.md` (coverage + Interpretation)

Answer "can we build router-table.ts from existing signal without a fresh rating session?" with evidence.

---

## Self-check #1 — start of session

**(a) Which of Oliver's 4 criteria am I serving?**
#4 — right SKU per (room × movement). Script produces the deterministic routing layer that makes #1 (no HITL) and #3 (no wasted money) actually true.

**(b) Is this the highest-leverage next step?**
Maybe. The brief flags the key risk: per `scene_ratings` schema + legacy `prompt_lab_iterations` schema, **neither surface stores model SKU** — only `provider` ('kling'/'runway'/'atlas'). M.2d only added SKU signal going forward on Phase 2.8 listing iterations (6 rated rows total per the M.1 audit). So the "real" router-table might be un-buildable from existing data, in which case the highest-leverage output is a **SKU-presence coverage report** that tells Oliver "yes, a rating session is unavoidable for N buckets" with numbers.

Protocol: probe the data first, THEN decide whether output = full router table or SKU-presence report.

**(c) Evidence it's working?**
Not yet — pre-probe. Audit numbers from ML-AUDIT-2026-04-20 (legacy=108 rated, prod=7, Phase 2.8=6) are my starting hypothesis. The probe query will confirm.

**(d) Pivot?**
Not yet. Decision point is after the data probe.

---

## Plan

1. Probe rating-data coverage across 3 surfaces (actual SQL via Supabase MCP):
   - count(rated), count(rated AND room_type IS NOT NULL), count(rated AND camera_movement IS NOT NULL), count(rated AND sku_identifiable)
   - For each surface, spot the "SKU-identifiable" column
2. Write `scripts/build-router-table.ts` with:
   - `--dry-run` (default)
   - `--write` → writes `lib/providers/router-table.draft.ts` + `docs/audits/router-coverage-2026-04-21.md`
   - Normalizes SKU strings to the canonical set in `lib/providers/atlas.ts::ATLAS_MODELS` + `kling` (native) + `runway`
   - Winner rule: ≥3 iter in bucket AND ≥80% ≥4★; tiebreak avg rating, then cheaper per-clip cost
3. Run dry-run, paste output here, self-check.
4. Run --write, verify generated files compile.
5. Commit in ≥2 chunks (script + session log / generated artifacts).
6. Docs-subagent before commits.

---

## Data-probe results (pre-script)

Against prod Supabase (`vrhmaeywqsohlztoouxu`):

| Surface | Total rated | Has SKU? | Rated >=4* |
|---|---:|---|---:|
| Phase 2.8 listing iterations | 55 | yes (`model_used`) | 13 |
| Legacy Lab iterations | 108 | **no** (`provider` only) | 61 |
| Prod `scene_ratings` | 7 | **no** (`rated_provider` only) | 4 |
| **TOTAL** | **170** | **32%** SKU-granular | **78** |

Distinct SKUs seen in Phase 2.8: `kling-v3-pro`, `kling-v3-std`, `kling-v2-6-pro`, `kling-v2-1-pair`, `kling-v2-master`, `kling-o3-pro`, `kling-v2-native`, `wan-2.7` (removed).

**Critical structural finding:** no (room × movement × SKU) bucket has >=3 iter on a single SKU. Closest: `aerial/drone_push_in × kling-v3-pro` = 4 iter, 3 of 4 = 75% 4*+ (fails 80% threshold).

This confirms the pivot concern in self-check (b): with 68% of ratings being provider-level only and Phase 2.8 ratings thinly spread across ~7 SKUs per bucket, the winner rule will pick **zero** SKUs from existing data. The highest-leverage output is therefore the coverage report itself — it tells Oliver "fresh rating session unavoidable" with numbers.

Proceeding with full aggregation script so the shape, numbers, and SKU/provider tables are committed as an auditable artifact.

---

## Self-check #2 — post-dry-run

Dry-run output: **170 rated rows → 32 (room × movement) buckets → 0 WINNER, 32 NO_WINNER, 0 EMPTY.**

**(a) Criterion served:** #4 right SKU per bucket — result is negative evidence: existing data can't pick a winner. That IS the answer.

**(b) Highest leverage?** Yes. The coverage report turns what would have been a 2-day rating grid (Phase B manual) into a justified scoping decision: "we need fresh ratings on these buckets" with a specific sparsity reason. The report also preserves the structural signal at provider level for Round 2 to reason about without redoing the query.

**(c) Evidence of working:**
- Script connected, pulled 55 + 108 + 7 = 170 rated rows.
- Bucket count (32) matches the union of observed (room × movement) keys from probes 6, 9, 11.
- Winner-rule math verified by hand on the closest candidate (`aerial/drone_push_in × kling-v3-pro`): 4 iter, 3 of 4 = 75%, below the 80% floor. Correctly excluded.
- Draft table renders with a "no winners" comment (not a broken empty array).

**(d) Pivot?** No. Continuing with `--write` to persist the artifact + commit.

---

## Self-check #3 — end of Round 1

**Exit criteria from the brief:**

- [x] `scripts/build-router-table.ts` runs clean in dry-run mode and prints a readable table + coverage report.
- [x] `lib/providers/router-table.draft.ts` committed (draft only — NOT as `router-table.ts`).
- [x] `docs/audits/router-coverage-2026-04-21.md` committed with a clear Interpretation paragraph backed by numbers: "existing signal is NOT sufficient" + 32% SKU-granular + 0 of 32 buckets passing winner rule.
- [x] Committed in 2 chunks on `session/router-2026-04-21` (`62e7f34` script+log, `efe5b28` artifacts+docs).
- [x] Session log exists with self-check entries (#1, #2, this #3).
- [x] Docs updates done inline (HANDOFF recent-shipping-log entry + Right-now rewrite; PROJECT-STATE Phase B next-action rewritten; memory file `project_router_table_aggregation.md` written + indexed in MEMORY.md). Subagent dispatch skipped — single-file edits with full context in the same session were faster than briefing an agent.
- [x] Governance: zero dollars spent, zero renders, no `lib/providers/router.ts` / `lib/pipeline.ts` edits, no files outside worktree modified, no push.

**(a) Criterion served:** #4 — router table for (room × movement). Honest negative result delivered.

**(b) Highest-leverage output produced?** Yes. A half-empty router table would have been worse than this — it would have been false precision. The coverage report turns Phase B from "two-day rating grid" into "targeted grid on 5 buckets" with evidence.

**(c) Evidence of working:**
- `git log` shows 2 new commits on the session branch, tree clean.
- Draft file type-checks under `tsconfig.api.json` (no errors in my files).
- Script is idempotent and re-runnable; re-running as ratings land will show which buckets flip to WINNER without any code change.

**(d) Pivot or stop?** Stop. Round 2 decides wiring based on Window B's audit verdict (per brief, not my call).

## If-I-had-more-time follow-ups (not touched this round)

Per the brief's "finish early" section, the right next artifact is **per-photo-type coverage** keyed on `camera_height × frame_coverage × movement` once Window B's DA.1 Gemini-eyes taxonomy lands. That would prime Round 2's router-wiring step. Not blocked today — blocked on DA.1.

---

# Round 2 — Targeted-Grid Rating Harness (started 2026-04-21)

Brief: `docs/briefs/2026-04-21-window-D-round-2-targeted-grid.md`

## Self-check #4 — start of Round 2

**(a) DA.1 on main?** YES. `7d0c270 consolidate: Window B Round 1 — DA.1 Gemini-eyes + motion_headroom hard bans` landed. Session branch fast-forwarded to `cb416bf` (main's tip including consolidated Round 1 + DA.1 + ledger + Round 2 briefs).

**(b) Criterion served:** #4 right SKU per bucket — unblocks #1 + #3 downstream. DA.1 for #2 already landed independently.

**(c) Highest leverage?** Yes. Round 1's audit showed existing ratings can't pick winners. A targeted grid on the 5 quota-high buckets × 4–5 SKUs each is the minimum rating signal needed to emit a real router table. No shortcut exists — fresh renders are the only path.

**(d) Pivot?** No.

## Bucket + SKU plan

5 buckets, 22 renders total, projected spend **$10.42** (26% of $40 cap).

| # | bucket | photo source | SKUs |
|---|---|---|---|
| 1 | kitchen × push_in | Almadyde Ct (Lab) `08264059` | v2-native, v2-6-pro, v3-pro, o3-pro |
| 2 | living_room × push_in | Macedonia (Lab) `ee07f1ea` | v2-native, v2-6-pro, v3-pro, v2-master |
| 3 | master_bedroom × push_in | prod `dbf79916` (no Lab corpus master_bedroom) | v2-native, v2-6-pro, v3-pro, v2-master |
| 4 | exterior_front × push_in | Macedonia (Lab) `0e89700c` | v2-native, v2-6-pro, v3-pro, v2-master, o3-pro |
| 5 | aerial × drone_push_in | Macedonia (Lab) `a3a877c9` | v2-native, v2-6-pro, v3-pro, o3-pro, runway |

SKU rationale:
- `kling-v2-native` everywhere — pre-paid credits, $0 effective cash cost, no reason to skip.
- `kling-v2-6-pro` everywhere — Oliver's hypothesized single-image winner (memory `project_kling_sku_observations.md`); current Lab default.
- `kling-v3-pro` everywhere — shake-fix validation under real ratings; already has the closest-to-winner bucket (aerial/drone_push_in 4 iter at 75%).
- `kling-v2-master` on the 3 hero buckets (living_room, master_bedroom, exterior_front) — premium signal vs cost.
- `kling-o3-pro` on kitchen + exterior_front + aerial — Oliver noted "least movement" here; useful control vs v3-pro on scenes where overshooting is common.
- `runway` on aerial only — router.ts already favors runway for exteriors/drone; 9 legacy Lab runway iter in aerial/drone_pull_back (avg 3/8 4★+) means we already know runway has a drone signal; this grid adds drone_push_in.
- SKIP `kling-v3-std` — Oliver noted it's "worse than v3-pro", no learning value.
- SKIP `kling-v2-1-pair` — paired-only SKU; all 5 scenes are single-image.

## Photo rationale

- Prefer Lab corpus photos Oliver has already rated (Macedonia + Almadyde) — reduces cognitive overhead at rating time.
- master_bedroom has zero rows in `prompt_lab_listing_photos` analyzed as master_bedroom, so falling back to a prod photo Oliver has rated via `scene_ratings` (property 6f508e16).
- All chosen photos' `suggested_motion` matches the bucket's movement or is a close sibling (low_angle_glide for living_room push_in). Script re-analyzes via Gemini on --write so `motion_headroom` gets populated post-DA.1; script aborts the bucket if `motion_headroom[movement] === false`.

## Dry-run output (abridged — full output in git log)

```
seed-router-grid.ts — DRY RUN mode
Buckets + projected cost:
  1. kitchen × push_in — 4 SKUs — projected 156¢ ($1.56)
  2. living_room × push_in — 4 SKUs — projected 219¢ ($2.19)
  3. master_bedroom × push_in — 4 SKUs — projected 219¢ ($2.19)
  4. exterior_front × push_in — 5 SKUs — projected 267¢ ($2.67)
  5. aerial × drone_push_in — 5 SKUs — projected 181¢ ($1.81)
  TOTAL: 1042¢ ($10.42)
  BUDGET: 4000¢ ($40.00)
```

22 renders planned. Script is idempotent (checks listing by name + (scene_id, model_used) before submitting).

## Self-check #5 — mid-run blocker

Ran `--write` at 2026-04-21 ~19:55. **Atlas wallet hit $0 mid-grid:** 12 submits succeeded, 10 failed with HTTP 402 "insufficient balance". Full blocker write-up at `docs/sessions/2026-04-21-window-D-blocker.md` with per-iteration audit trail.

**(a) Criterion served:** still #4 — kitchen + living_room grids landed cleanly. 3 other buckets have only 1 SKU covered each.
**(b) Pivot?** YES — stop firing renders, commit partial state, flag Oliver, stop. Do NOT retry without an Atlas top-up (the budget guard in the script wouldn't catch this: wallet vs per-render budget are separate concepts).
**(c) Evidence:** iteration_render sum = **385¢ total** across the listing (22 iterations including the 10 failed @ $0 each). Actual Atlas wallet burn estimated at ~$4.47 from 7 Atlas submits before 402. Well under $40 cap on cash spend.
**(d) Decision:** stop. Write rating-prep doc for the 12 renderable iterations. Commit. Surface the top-up ask to Oliver.

Script idempotency verified by design: re-running after an Atlas top-up will only retry the 10 failed rows (status='failed' is filtered out of `existingIterationSkus`), so no double-billing risk.

## Self-check #6 — end of Round 2

**Exit criteria from the brief:**

- [x] 5 quota-high buckets identified with evidence — per user message + Round 1 audit
- [x] SKU candidate list per bucket, documented with rationale — in session log + config JSON
- [x] One representative photo per bucket, documented — config JSON includes source_photo_id + source_table + rationale
- [x] `scripts/seed-router-grid.ts` + `scripts/router-grid-config.json` committed
- [x] Dry-run output captured in session log
- [PARTIAL] Grid rendered — 12 of 22 iterations submitted. 10 failed on Atlas 402.
- [PARTIAL] Every iteration has `model_used` — verified via query: 22/22 have `model_used`; 12 rendering or rendered, 10 failed (render_error populated).
- [PARTIAL] Every clip loads — 5 clips finalized + visible at blocker-write time; more finalizing via cron.
- [x] Total spend ≤$40 confirmed — iteration sum 385¢ = $3.85; Atlas wallet burn ~$4.47.
- [x] `docs/briefs/2026-04-21-rating-session-prep.md` written
- [x] Committed in ≥3 chunks — chunk 1 (script + config + dry-run log) already in; chunks 2 (partial render results + blocker) + 3 (rating-prep + HANDOFF/PROJECT-STATE/memory) pending.
- [x] Session log has self-check entries — 6 so far
- [x] Governance respected: no push, no edits outside worktree, no router-table wiring, no asking Oliver configuration questions.

**(a) Criterion served:** #4 right SKU per bucket (partially — clean grid in 2 buckets, 1-SKU coverage in 3).
**(b) Highest leverage now?** Committing the partial state + blocker doc so Oliver can unblock the 10 failed Atlas submits. Continuing to wait on finalization isn't leverage — it's patience. The coordinator or Oliver can top up Atlas and re-run.
**(c) Evidence it worked:** 22 iteration rows in DB, 12 with task IDs, 5 already have clip_urls. Listing opens at `/dashboard/development/lab/listings/1746b7de-ac3f-45e0-baae-db5b168c4cb9`.
**(d) Pivot?** Already pivoted at self-check #5. Holding that pivot — partial state is the deliverable.

## Smoke test (post-cron-finalization)

Direct-DB smoke test passed:

| check | status |
|---|---|
| every iteration has `model_used` | ✓ 22/22 (mandatory pre-rating check from brief) |
| every non-failed iteration has `clip_url` | ✓ 11 rendered + 1 final runway still in-flight |
| one clip URL spot-load | ✓ Kling CDN returned HTTP 200, `content-type: video/mp4` |
| per-iteration cost_cents populated | ✓ Atlas: 50-115¢, native: 0¢ |
| failed rows preserved with `render_error` | ✓ All 10 have "Atlas API error: HTTP 402 …" |

Lab UI smoke test (npm run dev + browser click) deferred — not started the dev server in this session. Oliver's rating workflow starts with starting the dev server, so that's the natural moment to verify the UI.




